import './ag-rt-monitor.js';

export default {
    title: 'Molecules/AgRtMonitor',
    tags: ['autodocs'],
};

const Template = () => {
    const el = document.createElement('ag-rt-monitor');
    return el;
};

export const Default = Template.bind({});
Default.args = {};
